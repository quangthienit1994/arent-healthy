import React from "react";

interface NavigateProps {
    title: string
    description: string;
    thumbnail: string
}

const Navigate: React.FC<NavigateProps> = ({ thumbnail, title, description }) => {
    return (

    )
}

export default Navigate;
